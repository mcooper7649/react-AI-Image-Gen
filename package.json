{
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "scripts": {
    "build": "cd client && npm run build",
    "start": "concurrently \"cd client && npm start\" \"cd server && npm start\""
  }
}
